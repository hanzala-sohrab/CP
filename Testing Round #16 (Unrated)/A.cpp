#include <bits/stdc++.h>
using namespace std;
int main () {
	cin.sync_with_stdio(false);
	cin.tie(0);
	int t, a, b;
	cin >> t;
	while (t--) {
		cin >> a >> b;
		cout << a + b << '\n';
	}
	return 0;
}
